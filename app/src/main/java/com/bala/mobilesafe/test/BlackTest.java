package com.bala.mobilesafe.test;

public class BlackTest /*extends AndroidTestCase */{
	
	/*public void testInsert(){
		BlackOpenHelper helper = new BlackOpenHelper(getContext());
		BlackDao dao = new BlackDao(helper);
		
		boolean insert = dao.insert("18800000000", 0);
		assertEquals(true, insert);
	}
	
	public void update(){
		BlackOpenHelper helper = new BlackOpenHelper(getContext());
		BlackDao dao = new BlackDao(helper);
		boolean update = dao.update("18800000000", 1);
		//断言
		assertEquals(true, update);
	}
	
	public void testGetAll(){
		BlackOpenHelper helper = new BlackOpenHelper(getContext());
		BlackDao dao = new BlackDao(helper);
		List<BlackBean> list = dao.getAllBlack();
		assertEquals(1, list.size());
	}
	
	public void testDelte(){
		BlackOpenHelper helper = new BlackOpenHelper(getContext());
		BlackDao dao = new BlackDao(helper);
		boolean delete = dao.delete("18800000000");
		assertEquals(true, delete);
	}
	
	public void addAll(){
		BlackOpenHelper helper = new BlackOpenHelper(getContext());
		BlackDao dao = new BlackDao(helper);
		for (int i = 0; i < 50; i++) {
			
			String phone = 18800000000L + i + "";
			int type = new Random().nextInt(3);// 0,1,2
			boolean insert = dao.insert(phone , type);
			assertEquals(true, insert);
		}
	}
	
	public void testFindType(){
		BlackOpenHelper helper = new BlackOpenHelper(getContext());
		BlackDao dao = new BlackDao(helper);
		int type = dao.getType("10086");
		assertEquals(1, type);
	}*/

}
